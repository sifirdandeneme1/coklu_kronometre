plugins {
    id 'com.android.application'
}

android {
    namespace 'com.omer.coklukronometre'
    compileSdk 33

    defaultConfig {
        applicationId "com.omer.coklukronometre"
        minSdk 21
        targetSdk 33
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'gradle-rules.pro'
        }
    }
}

dependencies {
    implementation 'androidx.appcompat:appcompat:1.6.1'
}